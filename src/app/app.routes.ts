import { Routes } from '@angular/router';
import { TableComponent } from './component/table/table.component';

export const routes: Routes = [
    {
        path: '', redirectTo: 'TableComponent', pathMatch: 'full'
    },];
